services:
  hidenseek:
    build: .
    ports:
      # Host port is configurable; the process inside the container always listens on 8080.
      - "${HIDENSEEK_PORT:-8082}:8080"
    environment:
      ASPNETCORE_URLS: http://0.0.0.0:8080
    restart: unless-stopped
